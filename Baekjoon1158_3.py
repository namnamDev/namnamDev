# N, K = map(int, input().split())
# li = list(range(1, N + 1))
# Q = []
# g = K
# start = 0
# while len(Q) != N:
#     start += g - 1
#     lenLi = len(li)
#     if lenLi <= start:
#         start = start % lenLi
#     Q.append(li.pop(start))
# an = str(Q)[1: -1]
# print("<" + an + ">")

print('thx!')
