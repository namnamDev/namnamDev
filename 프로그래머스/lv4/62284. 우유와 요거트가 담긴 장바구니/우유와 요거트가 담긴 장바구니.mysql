-- 코드를 입력하세요
SELECT A.CART_ID FROM (
    SELECT CART_ID, COUNT(DISTINCT CART_ID, NAME) AS C FROM CART_PRODUCTS 
    WHERE NAME IN ("Milk","Yogurt")
    GROUP BY CART_ID
) AS A
WHERE A.C >= 2
ORDER BY A.CART_ID